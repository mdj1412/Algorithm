#include <iostream>
#include <string>
#include <vector>

using namespace std;

int solution(vector<int> numbers, int target) {
    int answer = 0, mid_value;
    
    if (numbers.empty()) { return answer; }

    mid_value = numbers.back();


    return answer;
}

int main(void) {
    vector<int> v(10, 1);
    cout << v.back() << endl;
    cout << "hello world" << endl;

    return 0;
}